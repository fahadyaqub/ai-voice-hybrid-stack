# Skills Hub

This directory contains MCP adapters that bridge `AI_OS/skills/*` modules into callable MCP tools.

`skill_mcp_server.py` starts one MCP server per skill directory and delegates execution to the skill's executor.
