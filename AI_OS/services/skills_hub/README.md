# Skills Hub

This directory contains MCP adapters that bridge `AI_OS/skills/*` modules into callable MCP tools.

`skill_mcp_server.py` starts one MCP server that loads every skill in `AI_OS/skills/` and exposes each as a separate tool.
